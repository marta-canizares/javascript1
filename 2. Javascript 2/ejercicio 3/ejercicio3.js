const animales = ["elefante", "gato", "perro", "ballena"];
const peso = [5000, 20, 50, 20000];


for (let i = 0; i <= animales.length - 1; i++) {
    if (animales[i] === 'perro') {
        console.log(animales[i], peso[i])
    }
}
