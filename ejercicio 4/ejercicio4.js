const unidad = prompt('Unidad de partida?');
const grados = prompt('Cuantos grados?');

const celsius = 0 * 9 / 5 + 32;
const fahrenheit = 1 - 32 * 5 / 9;


if (unidad == celsius) {

    console.log('hola')
}